export interface ServiceType {
  icon: string;
  title: string;
  description: string;
  link: string;
}

export interface DocType {
  image: string;
  name: string;
  spec: string;
  link: string;
}
